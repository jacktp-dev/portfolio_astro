---


interface Props{
    category: string,
    place: string, 
    work: boolean,
    time: string,
    title: string, 
    description: string
}

const {category, place, work, time, title, description} = Astro.props;

const isWork = work ? 'bg-title-color text-white border-0':'bg-white text-title-color'
---

<article class="py-8 flex gap-8 justify-start max-w-screen-md">
    <section class="flex flex-col gap-2 w-48 justify-start items-start p-0 m-0">
        <span class=`inline-flex justify-center items-center px-4 py-1 border border-border-button rounded-full text-xs ${isWork}`>
            {category}
        </span>

        <h3 class="text-lg text-title-color">
            {place}
        </h3>

        <span class="text-sm text-text-color">
            {time}
        </span>
    </section>

    <section class="w-3/4 flex flex-col gap-2">
        <h2 class="text-lg text-title-color">
            {title}
        </h2>

        <p class="text-text-color">
            {description}
        </p>
    </section>


    </Button>

</article>